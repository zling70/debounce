{
  "name": "debounce",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "live-server --open=./index.html --host=localhost --port=8080 --proxy=/api:http://localhost:50001/api",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zling",
  "license": "ISC"
}
